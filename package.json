{
  "name": "qimen4ts",
  "version": "0.2.0",
  "description": "奇门遁甲排盘引擎（TypeScript）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run build:engine",
    "test": "node --test --import tsx test/*.test.ts",
    "build:engine": "esbuild src/cli.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/engine.cjs"
  },
  "license": "MIT",
  "dependencies": {
    "lunar-javascript": "^1.7.7",
    "taobi": "^0.4.5"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.3"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "NOTICE"
  ]
}
